apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: railcloud

resources:
  - namespace.yaml
  - deployments.yaml
  - services.yaml
  - ingress.yaml
  - hpa.yaml
  - pdb.yaml
  - configmaps.yaml
  - network-policy.yaml

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: railcloud-platform
